#include <stdio.h>

int main(void) {

    int x= 50, y=30;
    printf ("x가 y랑 같은가? %d\n", x == y);
    // 변수 == 변수 는 같은가? 임 참이면 1 오답이면 0
    printf ("x가 y랑 같은가? %d\n", x != y);
    // !=는 다른가를 물어봄
    printf ("x가 y보다 큰가? %d\n", x > y);
    // > = 왼쪽 변수가 큰지 물어봄    /  < = 오른쪽 변수가 큰지 물어봄
    printf ("x가 y랑 같은가? %d\n", x = y);
    // =는 x를 y로 대입하는 것

    if (x==y) {

    }
    else 
    {


    }
    //이면 ~ 아니면 ~ 코드
    return 0;

}